# angular-project

Steps to create  a Angular project

1. install the node
    npm install -g @angular/cli
2. check the node version
3. npm install -g @angular/cli
4.ng version
5.ng new my-angular-app
6.cd my-angular-app
7.ng serve

